spring.jpa.show_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.h2.console.enabled=true
spring.session.store-type=jdbc

#spring.profiles.include=oauth

# Test OAuth (originally reside in application-oauth.properties)
# The "test" environment automatically will use the application.properties file under the main if an additional file does not exist for the environment.
# However, the application-oauth.properties cannot be automatically shared. So, the following dummy lines are added.

spring.security.oauth2.client.registration.google.client-id=test
spring.security.oauth2.client.registration.google.client-secret=test
spring.security.oauth2.client.registration.google.scope=profile,email
